const myLibrary = [];

function Book(name) {
    // the constructor...
    this.name = name;
}

function addBookToLibrary(name) {
    temp = new Book(name);
    myLibrary.push(temp);
    console.log(myLibrary);
}

function displayLibrary() {
    array.forEach(element => {

    });
}